@model Fast.Web.Models.ChecklistModel
@using Fast.Web.Resources;
@using Newtonsoft.Json;

@section CSS_lib{
    <link href="~/Content/dashforge/lib/datatables.net-dt/css/jquery.dataTables.min.css" rel="stylesheet">
    <link href="~/Content/dashforge/lib/datatables.net-responsive-dt/css/responsive.dataTables.min.css" rel="stylesheet">
    <link href="~/Content/dashforge/lib/select2/css/select2.min.css" rel="stylesheet">
}

@section Styles{
    <style>
        .card-columns {
            column-count: 6;
        }
    </style>
}

@{
    List<Fast.Web.Models.ChecklistModel> checklists = (ViewBag.Checklists == null) ? new List<Fast.Web.Models.ChecklistModel>() : ViewBag.Checklists;
    var categories = checklists.Select(x => x.MenuTitle).Distinct().ToList();
    var counter = 0;
}

<div class="content content-fixed" style="min-width: 1290px !important;">
    <div class="container-fluid pd-x-0 pd-lg-x-10 pd-xl-x-0">
        <div class="d-sm-flex align-items-center justify-content-between mg-b-20 mg-lg-b-25 mg-xl-b-30">
            <div>
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb breadcrumb-style1 mg-b-10">
                        <li class="breadcrumb-item"><a href="@Url.Action("Index","Home")">Fast</a></li>
                        <li class="breadcrumb-item"><a href="@Url.Action("Index","Checklist")">CMS Checklist</a></li>
                        <li class="breadcrumb-item active" aria-current="page">Catalog</li>
                    </ol>
                </nav>
                <h4 class="mg-b-0 tx-spacing--1">Checklist Catalog</h4>
            </div>
            <div class="d-none d-md-block tx-right">
                <div>
                    <a href="#" id="display_grid" class="display-switch btn btn-primary btn btn-sm"><i class="fas fa-th-large"></i>&nbsp;Grid</a>
                    <!--
                    <a href="#" id="display_grid_category1" class="display-switch btn btn-secondary btn-sm"><i class="fas fa-th-large"></i>&nbsp;Grid Group By Menu Title V1</a>
                    <a href="#" id="display_grid_category2" class="display-switch btn btn-secondary btn-sm"><i class="fas fa-th-large"></i>&nbsp;Grid Group By Menu Title V2</a>
                    -->
                    <a href="#" id="display_list" class="display-switch btn btn-secondary btn btn-sm"><i class="fas fa-list"></i>&nbsp;List</a>
                    <select id="select_source" class="form-control wd-300-f d-inline">
                        <option value="">Show Checklists on My Location</option>
                        <option value="GetAll" @(ViewBag.FilterMode == 1 ? "selected" : "")>Show All Checklists</option>
                        <option value="Location" @(ViewBag.FilterMode == 2 ? "selected" : "")>Show by Filter Location</option>
                    </select>
                </div>
                <div id="location_detail" class="mg-t-10 wd-700" style="display:none">
                    <div class="row">
                        <div class="col-3 p-0"><select name="Location1" class="form-control selection-location"></select></div>
                        <div class="col-3 p-0"><select name="Location2" class="form-control selection-department"></select></div>
                        <div class="col-3 p-0"><select name="Location3" class="form-control selection-subdepartment"></select></div>
                        <div class="col-3 pd-t-0 pd-m-0"><button class="btn btn-primary w-100" id="search_by_location"><i data-feather="search" class="wd-10 mg-r-5"></i> SEARCH</button></div>
                    </div>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-12 mg-t-10">
                <div class="df-example demo-table">

                    <div class="row" id="view_grid">
                        <div class="col-sm-12 tx-right mg-b-20">
                            <input id="search_grid" class="form-control wd-300-f float-right" placeholder="Search Checklist" />
                        </div>
                        @if (ViewBag.Checklists != null)
                        {
                            foreach (var checklist in checklists)
                            {
                                <div class="col-sm-2 mg-b-20">
                                    <div class="card">
                                        <a href="@Url.Action("Generated/"+checklist.ID, "Checklist")">
                                            <img src="~/Uploads/checklist/icon/@((checklist.Icon == "" || checklist.Icon == null)?"_no_image.jpg":checklist.Icon)" class="card-img-top" alt="" style="min-height: 187px;max-height: 187px;">
                                            <div class="card-body text-center pd-10">
                                                <h6 class="card-title mg-b-0" style="min-height:34px">@checklist.MenuTitle</h6>
                                            </div>
                                        </a>
                                    </div>
                                </div>
                            }
                        }
                    </div>

                    @*
                    <div class="d-none" id="view_grid_category1">
                        <div class="accordion" id="accordion">
                            @if (ViewBag.Checklists != null)
                            {
                                foreach (var category in categories)
                                {
                                    <h6>@category</h6>
                                    <div class="pd-t-20">
                                        <div class="row">
                                            @foreach (Fast.Web.Models.ChecklistModel checklist in ViewBag.Checklists)
                                            {
                                                if (checklist.MenuTitle == category)
                                                {
                                                    <div class="col-sm-2 mg-b-20">
                                                        <div class="card">
                                                            <a href="@Url.Action("Generated/" + checklist.ID, "Checklist")">
                                                                <img src="~/Uploads/checklist/icon/@((checklist.Icon == "" || checklist.Icon == null) ? "_no_image.jpg" : checklist.Icon)" class="card-img-top" alt="" style="min-height: 187px;">
                                                                <div class="card-body text-center pd-10">
                                                                    <h6 class="card-title mg-b-0" style="min-height:34px">@checklist.Header</h6>
                                                                </div>
                                                            </a>
                                                        </div>
                                                    </div>
                                                }
                                            }
                                        </div>
                                    </div>
                                }
                            }
                        </div>
                    </div>

                    <div class="d-none" id="view_grid_category2">
                        @if (ViewBag.Checklists != null)
                        {
                            foreach (var category in categories)
                            {
                                <a class="btn-collapse btn btn-primary mg-b-6" data-toggle="collapse" href="#category@(counter)" role="button" aria-expanded="false" aria-controls="category@(counter)">@category</a>

                                counter++;
                            }

                            counter = 0;
                            foreach (var category in categories)
                            {
                                <div class="collapse mg-t-30" id="category@(counter)">
                                    <div class="row">
                                        @foreach (Fast.Web.Models.ChecklistModel checklist in ViewBag.Checklists)
                                        {
                                            if (checklist.MenuTitle == category)
                                            {
                                                <div class="col-sm-2 mg-b-20">
                                                    <div class="card">
                                                        <a href="@Url.Action("Generated/" + checklist.ID, "Checklist")">
                                                            <img src="~/Uploads/checklist/icon/@((checklist.Icon == "" || checklist.Icon == null) ? "_no_image.jpg" : checklist.Icon)" class="card-img-top" alt="" style="min-height: 187px;">
                                                            <div class="card-body text-center pd-10">
                                                                <h6 class="card-title mg-b-0" style="min-height:34px">@checklist.Header</h6>
                                                            </div>
                                                        </a>
                                                    </div>
                                                </div>
                                            }
                                        }
                                    </div>
                                </div>

                                counter++;
                            }
                        }
                    </div>
                    *@

                    <div class="card d-none" id="view_list">
                        <div class="card-body">
                            <table class="table table-bordered" id="do_datatable">
                                <thead class="thead-primary font-weight-bold">
                                    <tr>
                                        <th width="150px">Icon</th>
                                        <th>Menu Title</th>
                                        <th>Header</th>
                                        <th>Creator</th>
                                        <th width="200px">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @if (ViewBag.Checklists != null)
                                    {
                                        foreach (var checklist in checklists)
                                        {
                                        <tr>
                                            <td><img src="~/Uploads/checklist/icon/@((checklist.Icon == "" || checklist.Icon == null)?"_no_image.jpg":checklist.Icon)" class="card-img-top wd-100" alt=""></td>
                                            <td>@checklist.MenuTitle</td>
                                            <td>@checklist.Header</td>
                                            <td>@(checklist.Creator == null ? "-" : checklist.Creator.FullName)</td>
                                            <td class="">
                                            <a href="@Url.Action("Generated/"+checklist.ID, "Checklist")"><i class="fas fa-tasks"></i>&nbsp;Fill the Form</a><br />
                                            <a href="@Url.Action("Report/"+checklist.ID, "Checklist")"><i class="fas fa-chart-bar"></i>&nbsp;Report</a>
                                            </td>
                                        </tr>
                                        }
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div><!-- row -->
    </div><!-- container -->
</div><!-- content -->
<div id="jsonLocationTree" data-json='@Html.Raw(JsonConvert.SerializeObject(ViewBag.LocationTree))' />

@section Scripts{
    <script src="~/Content/dashforge/lib/jqueryui/jquery-ui.min.js"></script>
    <script src="~/Content/dashforge/lib/datatables.net/js/jquery.dataTables.min.js"></script>
    <script src="~/Content/dashforge/lib/datatables.net-dt/js/dataTables.dataTables.min.js"></script>
    <script src="~/Content/dashforge/lib/datatables.net-responsive/js/dataTables.responsive.min.js"></script>
    <script src="~/Content/dashforge/lib/datatables.net-responsive-dt/js/responsive.dataTables.min.js"></script>
    <script src="~/Content/dashforge/lib/select2/js/select2.min.js"></script>

    <script type="text/javascript">
        $(document).ready(function () {

            $('#search_grid').keyup(function () {
                var keyword = $(this).val().toLowerCase();
                $('#view_grid').find('.card').each(function () {
                    $this = $(this);
                    var h6 = $this.find('h6').first().text().toLowerCase();

                    if (h6.includes(keyword)) {
                        $this.parent().show();
                    } else {
                        $this.parent().hide();
                    }
                })
            })

            $('#select_source').change(function () {
                var url = "@(Url.Action("Catalog","Checklist"))";
                if ($(this).val() == "GetAll") {
                    url += "?data=GetAll";
                    window.location = url;
                } else if ($(this).val() == "Location") {
                    $('#location_detail').show();
                } else {
                    window.location = url;
                }
            })

            $('#search_by_location').click(function () {
                var url = "@(Url.Action("Catalog","Checklist"))?data=Location&location1="+$('.selection-location').val()+"&location2="+$('.selection-department').val()+"&location3="+$('.selection-subdepartment').val();
                window.location = url;
            })

            $('#accordion').accordion({
                heightStyle: 'content'
            });

            $('.btn-collapse').click( function(e) {
                $('.collapse').collapse('hide');
            });

            $('#do_datatable').DataTable({
                order: [],
                //responsive: true,
                language: {
                    searchPlaceholder: 'Search...',
                    sSearch: '',
                    lengthMenu: '_MENU_ items/page',
                }
            });

            //$('.dataTables_length select').select2({ minimumResultsForSearch: Infinity });

            $('.display-switch').click(function () {
                $this = $(this);
                $(document).find('.display-switch').each(function () {
                    var view = $(this).attr('id');
                    view = view.replace("display", "view");

                    if ($this.attr('id') == $(this).attr('id')) {
                        $(this).removeClass('btn-secondary').addClass('btn-primary');
                        $('#'+view).removeClass('d-none')
                    } else {
                        $(this).removeClass('btn-primary').addClass('btn-secondary');
                        $('#'+view).addClass('d-none');
                    }
                });
            })


            var display = getUrlParameter('show');
            if (display == "list") {
                $('#display_list').trigger('click');
            }

            var LocationTree = JSON.parse($("#jsonLocationTree").attr("data-json"));

            console.log(LocationTree);

            var select_locat = $('select.selection-location');
            select_locat.append('<option label="Choose "></option>');

            for (var i = 0; i < LocationTree.ProductionCenters.length; i++) {
                select_locat.append('<option value='+LocationTree.ProductionCenters[i].LocationID+'>' + LocationTree.ProductionCenters[i].Description + '</option>')
            }

            $('.selection-location').select2({
                width: "100%",
              placeholder: 'Select Location',
              searchInputPlaceholder: 'Search options'
            });

            $('.selection-department, .selection-subdepartment').select2({width: "100%"});

            var locat_selected;
            $(document).on("change",".selection-location",function(){
                locat_selected = $(this).select2("val");
                console.log(locat_selected);

                $('.selection-department').select2('destroy');
                $('.selection-subdepartment').select2('destroy');
                $('.selection-department').empty();
                $('.selection-subdepartment').empty();

                var select_depart = $('select.selection-department');
                select_depart.append('<option label="Choose "></option>');

                for (var i = 0; i < LocationTree.ProductionCenters.length; i++) {
                    if ($.inArray(LocationTree.ProductionCenters[i].LocationID.toString(), locat_selected) >= 0) {
                        select_depart.append('<optgroup label="'+LocationTree.ProductionCenters[i].Description+'">')
                        for (var j = 0; j < LocationTree.ProductionCenters[i].Departments.length; j++) {
                            select_depart.append('<option value='+LocationTree.ProductionCenters[i].Departments[j].LocationID +'>' + LocationTree.ProductionCenters[i].Code+"-"+LocationTree.ProductionCenters[i].Departments[j].Description + '</option>')
                        }
                        select_depart.append('</optgroup>')
                    }
                }

                $('.selection-department').select2({
                    width: "100%",
                  placeholder: 'Select Department',
                  searchInputPlaceholder: 'Search options'
                });
                $('.selection-subdepartment').select2();
            });

            $(document).on("change",".selection-department",function(){
                var department_selected = $(this).select2("val");
                console.log(department_selected);

                $('.selection-subdepartment').select2('destroy');
                $('.selection-subdepartment').empty();

                var select_subdepart = $('select.selection-subdepartment');
                select_subdepart.append('<option label="Choose "></option>');

                for (var i = 0; i < LocationTree.ProductionCenters.length; i++) {
                    if ($.inArray(LocationTree.ProductionCenters[i].LocationID.toString(), locat_selected) >= 0) {
                        for (var j = 0; j < LocationTree.ProductionCenters[i].Departments.length; j++) {
                            if ($.inArray(LocationTree.ProductionCenters[i].Departments[j].LocationID.toString(), department_selected) >= 0) {
                                select_subdepart.append('<optgroup label="' + LocationTree.ProductionCenters[i].Description+ " - " + LocationTree.ProductionCenters[i].Departments[j].Description + '">')
                                for (var k = 0; k < LocationTree.ProductionCenters[i].Departments[j].SubDepartments.length; k++) {
                                    select_subdepart.append('<option value='+LocationTree.ProductionCenters[i].Departments[j].SubDepartments[k].LocationID +'>' + LocationTree.ProductionCenters[i].Code+"-"+LocationTree.ProductionCenters[i].Departments[j].Code+"-"+LocationTree.ProductionCenters[i].Departments[j].SubDepartments[k].Description + '</option>')
                                }
                                select_subdepart.append('</optgroup>')
                            }
                        }
                    }
                }

                $('.selection-subdepartment').select2({
                    width: "100%",
                  placeholder: 'Select Sub Department',
                  searchInputPlaceholder: 'Search options'
                });
            });


            var location1 = getUrlParameter('location1');
            if (typeof location1 !== "undefined") {
                $('#location_detail').show();
                $('.selection-location').val(location1).change();

                var location2 = getUrlParameter('location2');
                if (typeof location2 !== "undefined") {
                    $('.selection-department').val(location2).change();

                    var location3 = getUrlParameter('location3');
                    if (typeof location3 !== "undefined") {
                        $('.selection-subdepartment').val(location3).change();
                    }
                }
            }
        });
    </script>
}